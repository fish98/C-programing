#include <stdio.h>

int main(){
    printf("hello");
    return 0;
}

int a[10000]; || int * a = (int*)malloc(sizeof(int)*10000)

// typedef struct fish node;
struct fish{
    int value;
    node* next;
}
struct fish a; || node * a = (node*)malloc(sizeof(node));
a.value = 2
a -> value = 1
a->value

node a[10]
node** a = (node*)malloc(sizeof(node*)*10)

a[3]->value
sizeof(a)/sizeof(a[0])
a[3].value
node a;
a.value;
node* a;
a->value;
//a.valued